1. Staging 
 
git init -> to make a folder into repo

git config --global user.name "Nabil"
git config --global user.email "nabil.uiu.bd@gmail.com"

git config user.name "Nabil"
git config user.email "nabil.uiu.bd@gmail.com"

git status

git add commands.txt

git add -A / git add --all / git add .  -> all means the same

git add * -> stage all without the deleted one 

git reset -> to unstage 

2. commit

git commit # after this you have to type comment to commit then press "escape" then ":x"

git commit -m "comment" # easier

git reset HEAD~ -> rollback to stage / uncommit 

3. Log

git log -> It will show the chnages we made

git log -p -2 -> it will show the last 2 changes 

git log --oneline -> it will show the changes in one line

git checkout f637962 -> f637962 ei commit e niye jabe 

git checkout master -> Master branch e chole jabo 

git diff -> shows the changes with the last stage 

git diff f637962 c2324353 -> will show the changes comparison of these two stage.

git show f637962 -> it will show the change of the stage f637962 

4 . Remove

git rm nbl.txt -> remove nbl.txt 

git rm nbl.txt -f -> It will remove forcefully 

git reset Head nbl.txt -> after deletation it will not even track 

git rm chached nbr.txt ->

#if you delete manally from local , then you just delete then -> git add .

git reset -> it will rollback but not restore 

git reset --hard -> it will rollback and restore the deleted file.



5. Push to github

git remote add origin https://github.com/nabilhasanbd/Git_demo.git -> sync the local repository with github

git branch -m main -> renaming the master into main

git push -u origin master / git push -u origin main -> push to github 


6. Branching

git branch -> show branch

git branch branch2 -> create branch named branch2

git checkout branch2 -> switch to branch2

git branch -d branch2 -> delete branch2

git push origin --delete branch2 -> delete branch remotely


7. Merge

git merge master -m "Merging dev with master"

git merge master -m "Merging dev with master" 

git merge dev -> Everything of the dev branch will be merged with master

8. Push

git push origin master -> sending the origin master branch to remote repo

git push origin dev /  git push origin staging -> sending the origin dev/staging branch to remote repo

9. Fetch

git fetch -> Local repo informed that the change from remote repo

# after fetching if we write -> git merge then we will be able to see the change we made in remote repo

10. Pull

# pull = git fetch + git merge 

git pull 
























